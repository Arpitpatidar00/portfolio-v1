import { ProjectsPage } from "@/modules/projectsPage";

export const metadata = {
  title: "Projects | Arpit Patidar",
  description: "A showcase of selected works and projects built by Arpit Patidar.",
};

export default function ProjectsRoute() {
  return <ProjectsPage />;
}
